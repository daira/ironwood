-- The circuit–verifier bridge: modules importing both `Zcash.Circuits` and `Zcash.Snark`,
-- connecting the proved Action circuit semantics to the verifier soundness stack.

import Zcash.Bridge.VkProjection
